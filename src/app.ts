/**
 *
 */
const dotenv = require("dotenv");
const fs = require("fs");
const path = require("path");

dotenv.config();

/**
 * Event handler code
 */

/**
 * Command handler code
 */

/**
 * Client login
 */
